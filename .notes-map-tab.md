# Map tab implementation notes (internal)

## Key infra facts
- Map component: `client/src/components/Map.tsx` exports `MapView` ({ initialCenter, initialZoom, onMapReady, className }). Loads Google Maps via forge proxy with libraries marker,places,geocoding,geometry. Use `google.maps.marker.AdvancedMarkerElement({ map, position, title, content })` for custom keepsake markers. mapId "DEMO_MAP_ID".
- Shared sync: `useSharedState<T>(key, default)` from `client/src/hooks/useSyncedData.ts` — single trpc.sharedState.all cache, optimistic writes, object-valued state patched via diffMaps + sharedState.patch (conflict-safe per key). Store place log as one keyed object: `{ [entryId]: PlaceLogEntry }`.
- Photos: server `photos` router (list/upload/remove) in `server/routers.ts` lines 131-170; upload takes base64 + caption/date/creator, stores in S3 via storagePut; NO place field currently. `PhotoJournal.tsx` has compressImage (lines 24-51) + upload dialog patterns to reuse.
- photos table (drizzle/schema.ts lines 73-87): fileKey, url, caption, date, createdBy..., no place tag. Option: add `place` varchar column OR tag via caption; better: add nullable `placeId` column + migration.
- Bottom nav: `client/src/components/AppShell.tsx` TABS array (5 tabs, grid-cols-5): Home /, Chapters /handbook, Trackers /trackers, 100 Things /handbook/100-things, Memories /memories. Add Map tab -> grid-cols-6, icon `Map` or `MapPin` from lucide. Maybe replace "100 Things" label size ok.
- Routes: `client/src/App.tsx` Router switch; add `<Route path="/map" component={ParkMap} />`.
- Design tokens: paper #F8F3EB, ivory #FFFDF8, ink navy #22364D, sienna #C66A3D / #B4512E, moss #7B8C6A, border #E5DAC8; classes: sticker-card, keepsake-card, btn-ink, press-scale, eyebrow, font-display (Cormorant Garamond), font-body. PageShell/PageHeader/Eyebrow/PawDivider from AppShell.
- Tests live in server/*.test.ts (vitest), suite currently 90/90.
- Checkpoint 4cc29ca3 = auto-archive. GitHub remote `github` works (connector enabled). Push: `git push github main`.
- Live domain: wobblesapp-2cxvdpqb.manus.space. Auto-publish on checkpoint.

## Parks to pre-pin (coords approx, Singapore)
- Home base: Blk 587 Woodlands Drive 16 → 1.42845, 103.79529 (OneMap verified, postal 730587)
- Small park next to the block (neighbourhood park by Blk 587) → ~1.4288, 103.7946 (label "Park next to the block")
- Woodlands Waterfront Park (dog run, 24/7, sea views) → 1.4527, 103.7786 (Wikipedia verified)
- Admiralty Park (NParks, 31 Riverside Rd) → 1.4464, 103.7807 (Wikipedia/latlong verified)
- Sembawang Park dog run (near Wak Hassan Pl, northern tip Sembawang Rd) → 1.4620, 103.8365
Center map ~1.4450, 103.795, zoom ~13.

## Data model (shared state key "placeLog")
```ts
type PlaceLogEntry = {
  id: string;
  placeId: string;      // slug of PLACES
  kind: "visit" | "journal";
  date: string;         // YYYY-MM-DD
  text?: string;        // journal body / visit note
  person?: "marcus" | "chesa";
  createdAt: number;
};
```
- Visits = entries kind visit; place "been here" = has >=1 visit.
- Photos tagged: add `placeId` col to photos table (nullable varchar 64), extend photos.upload input + list output, PhotoJournal keeps working (placeId undefined). Place sheet uploads with placeId set and filters list by placeId client-side.

## Content file `client/src/content/places.ts`
PLACES: id, name, area (Woodlands/Admiralty/Sembawang), lat, lng, emoji, blurb, tags (dog run etc), driveTime.

## UI plan
- Page `client/src/pages/ParkMap.tsx`: PageHeader "Wobbles' Map", MapView h-[calc] with markers; keepsake marker = custom div (emoji pin, sienna/ink, visited = moss fill w/ ✓ count). Tap marker -> bottom Drawer place sheet: name, blurb, visit count + "Log a visit today" btn, journal entries list + add form, photos grid (tagged) + add photo button (reuse compress+upload w/ placeId).
- Horizontal chip list of places under map for non-map access.

## Progress so far (phase 1-2)
- DONE: `client/src/content/places.ts` (PLACES, HOME_CENTER, MAP_CENTER 1.4462/103.7985, MAP_ZOOM 13, getPlace). 5 places incl home-blk-587 (isHome), park-next-to-block, woodlands-waterfront, admiralty-park, sembawang-dog-run.
- DONE: `client/src/lib/placeLog.ts` — PlaceLogEntry {id, placeId, kind visit|journal, date, text?, by?, createdAt}, normalizePlaceLog, entriesForPlace, visitCount (distinct days), lastVisit, hasVisited, journalForPlace, makeEntry. Shared state key "placeLog" via useSharedState (object map → patch-safe).
- DONE: photos schema + DB: added nullable `placeId` varchar(64) to photos table (drizzle/schema.ts) AND applied ALTER TABLE via webdev_execute_sql. routers.ts photos.upload input now takes optional placeId, passed to db.addPhoto (InsertPhoto covers it).
- PhotoJournal.tsx reuse: compressImage(file) at top of file (exported? NO — currently module-private, need to export it or duplicate). Upload via trpc.photos.upload.useMutation with {fileName, mimeType, dataBase64, caption?, date, placeId?}. PhotoLightbox component exists (client/src/components/PhotoLightbox.tsx) takes {photos, index, onIndexChange, onClose, onDelete}.
- todayISO from "@/lib/dates"; formatDate from "@/content/wobbles".

## Remaining
1. Export compressImage from PhotoJournal.tsx (or move to lib) for reuse.
2. Build `client/src/pages/ParkMap.tsx`: MapView + AdvancedMarkerElement custom pins (emoji + visited moss ring), place bottom-sheet (shadcn Drawer or Dialog) with blurb/tags/travel, visit log button ("Wobbles was here today"), journal add/list, tagged photo grid + upload w/ placeId, delete own entries.
3. Bottom nav: AppShell.tsx TABS add { href: "/map", label: "Map", icon: MapPin } → grid-cols-6.
4. Route in App.tsx: <Route path="/map" component={ParkMap} />.
5. Tests: server/placeLog.test.ts style — but placeLog.ts is client lib; existing pattern: server/householdSettings.test.ts imports from client lib? CHECK: householdSettings lib lives in client/src/lib/householdSettings.ts and tests in server/householdSettings.test.ts import it directly (vitest allows). Do same: server/placeLog.test.ts importing @/lib/placeLog? vitest.config resolves? Check vitest.config.ts aliases; householdSettings.test imports via relative path "../client/src/lib/householdSettings" most likely.
6. tsc, pnpm test, build, screenshot /map, checkpoint, push github, deliver.

## Progress update 2
- DONE: compressImage exported from PhotoJournal.tsx.
- DONE: client/src/pages/ParkMap.tsx created — MapView (h-340px, controls off), emoji pin markers via AdvancedMarkerElement with moss ring when visited, "x/4 explored" badge, place list cards, PlaceSheet drawer (visit log button + chips w/ delete, photo grid + upload tagged placeId, journal add/list/delete). Uses PhotoLightbox for viewer (check its LightboxPhoto iface matches PhotoRowLite: id,url,caption,date...).
- REMAINING:
  1. AppShell.tsx: TABS add { href: "/map", label: "Map", icon: MapPin } (import MapPin), grid-cols-5 → grid-cols-6; maybe shorten labels.
  2. App.tsx: import ParkMap, add <Route path="/map" component={ParkMap} />.
  3. Check Textarea component exists at client/src/components/ui/textarea.tsx.
  4. Tests: server/placeLog.test.ts (import "../client/src/lib/placeLog"), cover normalizePlaceLog garbage tolerance, visitCount distinct days, lastVisit, entriesForPlace sort, makeEntry trims.
  5. tsc --noEmit, pnpm test, pnpm build, screenshot /map (mobile 375x812), todo.md ticks, checkpoint, git push github, deliver.
- Home place isHome hides visit button. MAP_CENTER 1.4462/103.7985 zoom 13.

## Progress update 3
- DONE: AppShell TABS has /map MapPin, grid-cols-6 px-1. App.tsx route /map → ParkMap added.
- PhotoLightbox LightboxPhoto {id,url,date,caption?,createdByName?} — PhotoRowLite compatible ✓. Textarea ui component exists ✓.
- NEXT: write server/placeLog.test.ts (import "../client/src/lib/placeLog"; pattern like householdSettings.test.ts) → tsc --noEmit → pnpm test → pnpm build → screenshot /map mobile 375x812 → todo.md tick Map items → checkpoint → git push github wobbles-handbook (remote "github" exists; connector enabled earlier) → deliver result.
- Live domain: wobblesapp-2cxvdpqb.manus.space

## Progress update 4 — verification
- tsc clean, tests 99/99 pass (placeLog.test.ts 9 new), pnpm build clean.
- Screenshot: /map page layout correct (header, 0/4 explored badge, 5 place cards, MAP tab active in 6-col nav). Map tiles blank in screenshot tool ONLY.
- ROOT CAUSE (webdev_debug high confidence): screenshot/preview renderer loads via 127.0.0.1:3000; Forge maps proxy rejects that Origin (401). Curl w/ public preview Origin → 200 + real Maps JS. So map WILL render on public preview + published site. Not a code bug.
- Hardening added to Map.tsx: guard window.google?.maps after loadMapScript.
- AuthGate blocks sandbox browser check on public URL (sign-in wall) — expected; can't easily verify visually without login. Confidence from curl Origin tests is sufficient.
- Known pre-existing console warning: duplicate key "2026-08-21" on Home (two milestones same date) — NOT from map feature. Consider fixing: key uses m.date; two milestones share 2026-08-21. Fix by key={`${m.date}-${m.label}`} in Home.tsx Coming up section.
- REMAINING: fix duplicate key in Home.tsx, tick todo.md map items, checkpoint, git push github, deliver result.
